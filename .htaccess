# 1. Block access to the .git folder for security
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^\.git/ - [F,L]
</IfModule>

# 2. Block direct access to all hidden files (starting with a dot)
<FilesMatch "^\.">
    Require all denied
</FilesMatch>